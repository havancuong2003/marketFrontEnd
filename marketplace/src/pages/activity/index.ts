import { withClasses } from "../../components/common/with-classes"
import { Activities as AC } from "./activity"
import classes from "./activity.module.scss"

export const Activities = withClasses(classes, AC)
