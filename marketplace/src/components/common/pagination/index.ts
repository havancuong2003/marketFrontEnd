import { withClasses } from "../with-classes"
import { PaginationActivity as p } from "./pagination"
import classes from "./pagination.module.scss"

export const PaginationActivity = withClasses(classes, p)