import { withClasses } from "../../common/with-classes";
import { ChangePassword as CPW } from "./change-password";
import classes from "./change-password.module.scss";

export const ChangePassword = withClasses(classes, CPW);
