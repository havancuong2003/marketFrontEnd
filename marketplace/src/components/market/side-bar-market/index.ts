import { withClasses } from "../../common/with-classes";
import { SideBar as SB } from "./side-bar-market";
import classes from "./side-bar-market.module.scss";

export const SideBar = withClasses(classes, SB);
