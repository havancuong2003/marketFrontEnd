import { withClasses } from "../../common/with-classes";
import { ListingSucceeded as LS } from "./listting-succeeded";
import classes from "./listing.module.scss";

export const ListingSucceeded = withClasses(classes, LS);
