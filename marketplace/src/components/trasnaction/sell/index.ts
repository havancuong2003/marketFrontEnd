import { withClasses } from "../../common/with-classes";
import { SellHero as SH } from "./sell-hero";
import classes from "./sell-hero.module.scss";

export const SellHero = withClasses(classes, SH);
