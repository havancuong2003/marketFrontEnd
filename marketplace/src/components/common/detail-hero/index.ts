import { withClasses } from "../with-classes";
import { DetailHero as DH } from "../detail-hero/detail-hero";
import classes from "./detail-hero.module.scss";

export const DetailHero = withClasses(classes, DH);
