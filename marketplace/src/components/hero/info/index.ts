import { withClasses } from "../../common/with-classes";
import { InfoHero as IH } from "./info-hero";
import classes from "./info-hero.module.scss";
export const InfoHero = withClasses(classes, IH);
