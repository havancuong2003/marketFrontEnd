import { withClasses } from "../common/with-classes";
import { HistoryTrans as HT } from "./list-history-trans";
import classes from "./history-trans.module.scss";

export const HistoryTrans = withClasses(classes, HT);
