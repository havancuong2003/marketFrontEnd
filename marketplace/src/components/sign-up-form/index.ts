import { withClasses } from "../common/with-classes"
import { SignUpForm as SF } from "./sign-up-form"
import classes from "./sign-up-form.module.scss"

export const SignUpForm = withClasses(classes, SF)
