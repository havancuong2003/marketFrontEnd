import { withClasses } from "../../with-classes";
import { ButtonBack as B } from "./button-back";
import classes from "./button-back.module.scss";

export const ButtonBack = withClasses(classes, B);
