import { withClasses } from "../../components/common/with-classes"
import { StartPage as SP } from "./start-page"
import classes from "./start-page.module.scss"

export const StartPage = withClasses(classes, SP)
