import { withClasses } from "../../common/with-classes";
import { PaySucceeded as PS } from "./pay-succeeded";
import classes from "./pay.module.scss";

export const PaySucceeded = withClasses(classes, PS);
